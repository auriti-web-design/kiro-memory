{
  "name": "totalrecall",
  "displayName": "Total Recall",
  "description": "Persistent cross-session memory for AI coding assistants",
  "version": "0.1.0",
  "publisher": "auriti-labs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Auriti-Labs/totalrecall"
  },
  "bugs": {
    "url": "https://github.com/Auriti-Labs/totalrecall/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "keywords": ["kiro", "memory", "ai", "context", "coding-assistant"],
  "icon": "resources/icons/totalrecall.png",
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "totalrecall-sidebar",
          "title": "Total Recall",
          "icon": "resources/icons/totalrecall.svg"
        }
      ]
    },
    "views": {
      "totalrecall-sidebar": [
        {
          "id": "kiroMemory.projects",
          "name": "Projects",
          "icon": "resources/icons/totalrecall.svg",
          "contextualTitle": "Total Recall — Projects"
        },
        {
          "id": "kiroMemory.observations",
          "name": "Observations",
          "icon": "resources/icons/observation.svg",
          "contextualTitle": "Total Recall — Observations"
        },
        {
          "id": "kiroMemory.sessions",
          "name": "Sessions",
          "contextualTitle": "Total Recall — Sessions"
        }
      ]
    },
    "commands": [
      {
        "command": "kiroMemory.search",
        "title": "Total Recall: Search",
        "icon": "$(search)"
      },
      {
        "command": "kiroMemory.showDashboard",
        "title": "Total Recall: Open Dashboard",
        "icon": "$(browser)"
      },
      {
        "command": "kiroMemory.showContext",
        "title": "Total Recall: Show Context for Current Project",
        "icon": "$(info)"
      },
      {
        "command": "kiroMemory.refresh",
        "title": "Total Recall: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "kiroMemory.quickSearch",
        "title": "Total Recall: Quick Search (Ctrl+Shift+K)",
        "icon": "$(zap)"
      },
      {
        "command": "kiroMemory.showFileObservations",
        "title": "Total Recall: Show File Observations"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "kiroMemory.refresh",
          "when": "view == kiroMemory.projects",
          "group": "navigation"
        },
        {
          "command": "kiroMemory.refresh",
          "when": "view == kiroMemory.observations",
          "group": "navigation"
        },
        {
          "command": "kiroMemory.refresh",
          "when": "view == kiroMemory.sessions",
          "group": "navigation"
        },
        {
          "command": "kiroMemory.search",
          "when": "view == kiroMemory.observations",
          "group": "navigation"
        },
        {
          "command": "kiroMemory.showDashboard",
          "when": "view == kiroMemory.projects",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "kiroMemory.search"
        },
        {
          "command": "kiroMemory.showDashboard"
        },
        {
          "command": "kiroMemory.showContext"
        },
        {
          "command": "kiroMemory.refresh"
        },
        {
          "command": "kiroMemory.quickSearch"
        },
        {
          "command": "kiroMemory.showFileObservations",
          "when": "false"
        }
      ]
    },
    "keybindings": [
      {
        "command": "kiroMemory.quickSearch",
        "key": "ctrl+shift+k",
        "mac": "cmd+shift+k"
      }
    ],
    "configuration": {
      "title": "Total Recall",
      "properties": {
        "kiroMemory.workerHost": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host del worker Total Recall (default: 127.0.0.1)"
        },
        "kiroMemory.workerPort": {
          "type": "number",
          "default": 3001,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Porta del worker Total Recall (default: 3001)"
        },
        "kiroMemory.refreshInterval": {
          "type": "number",
          "default": 30,
          "minimum": 10,
          "maximum": 300,
          "description": "Intervallo di aggiornamento automatico in secondi (default: 30)"
        },
        "kiroMemory.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic periodic refresh of tree views and status bar"
        },
        "kiroMemory.maxObservations": {
          "type": "number",
          "default": 50,
          "minimum": 10,
          "maximum": 200,
          "description": "Numero massimo di osservazioni da mostrare nella tree view (default: 50)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./tsconfig.json",
    "watch": "tsc -watch -p ./tsconfig.json",
    "lint": "eslint src --ext ts",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^2.24.0"
  }
}
